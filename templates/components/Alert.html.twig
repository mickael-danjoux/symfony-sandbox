<div class="{{ this.classes }}" role="alert">
    {% block content %}
            {{ message }}
    {% endblock %}
    {% if not withoutCloseButton %}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    {% endif %}
</div>
