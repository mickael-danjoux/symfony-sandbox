{% extends 'base.html.twig' %}

{% block title %}Hello InvoiceController!{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12 d-flex justify-content-center">
            <h1>Invoice component</h1>
        </div>
    </div>
    <div class="shadow-blur shadow-blur--rainbow p-4">
        <div class="row">
            <div class="col-12">
                {{ component('Invoice:InvoiceForm', {
                    initialInvoice: invoice,
                }) }}
            </div>
        </div>
    </div>
{% endblock %}
